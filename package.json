{
  "name": "microservice-mapper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "cd server && npm install && cd ../client && npm install && cd ../remote-collector && npm install && cd ../traffic-generator && npm install",
    "traffic:sockshop": "cd traffic-generator && npm run sockshop",
    "traffic:vertikal": "cd traffic-generator && npm run vertikal",
    "traffic:all": "cd traffic-generator && npm run all",
    "export-telemetry": "server/node_modules/.bin/tsx scripts/export_vectorized_telemetry.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
