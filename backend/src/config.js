export default {
  DOCKER_SOCKET: '/var/run/docker.sock',
  PORT: 3001,
  POLLING_INTERVAL_MS: 5000,
  METRIC_HISTORY_SIZE: 720,
  METRIC_HISTORY_WINDOW_MS: 3600000,
  SOCK_SHOP_PREFIXES: ['sock-shop', 'sockshop', 'sock_shop', 'weaveworksdemos'],
  VERTIKAL_PREFIXES: ['vertikal', 'supabase']
};
